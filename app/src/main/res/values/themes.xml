<resources>
    <style name="Theme.TimerApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">#7B1E1E</item>
        <item name="colorPrimaryVariant">#5C1616</item>
        <item name="colorOnPrimary">#FFFFFF</item>
    </style>

    <!-- Tema transparan tanpa animasi untuk PopupTrampolineActivity -->
    <style name="Theme.TimerApp.Trampoline" parent="@android:style/Theme.Translucent.NoTitleBar">
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!-- Tema platform (bukan tema app) untuk membungkus context Service lewat
         ContextThemeWrapper saat inflate popup "Atur durasi", supaya
         NumberPicker bawaan tampil dengan warna yang benar. Dipilih di kode
         sesuai uiMode sistem (lihat OverlayPopupService). -->
    <style name="Theme.Popup.Light" parent="@android:style/Theme.Material.Light" />
    <style name="Theme.Popup.Dark" parent="@android:style/Theme.Material" />
</resources>
