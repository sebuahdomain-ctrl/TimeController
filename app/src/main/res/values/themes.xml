<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.TimeController" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/maroon</item>
        <item name="colorPrimaryDark">@color/maroon_dark</item>
        <item name="colorAccent">@color/maroon_light</item>
        <item name="android:statusBarColor">@color/bg_white</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
    </style>

    <style name="Theme.Transparent" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <!-- Tema untuk NumberPicker (wheel menit/detik) supaya angka aktif putih tebal,
         mendekati styling wheel di desain HTML (angka tengah solid & jernih). -->
    <style name="WheelPickerTheme" parent="Theme.MaterialComponents.DayNight">
        <item name="colorControlNormal">@color/text_gray</item>
        <item name="android:textColorPrimary">@color/text_white</item>
        <item name="android:colorControlNormal">@color/border_white_5</item>
    </style>
</resources>
